{
  "config": {
    "step": {
      "user": {
        "title": "Connect to the Intelbras panel",
        "description": "Enter the panel's IP and password. The model (AMT 2018/1016/4010) is detected automatically.\n\nBy default, Home Assistant does NOT ask for a code to arm or disarm from the UI — the password below is used automatically behind the scenes, like an already logged-in app. Only check the options below if you want Home Assistant itself to prompt for the password before arming and/or disarming (e.g. if several people share the same Home Assistant and you want to confirm who's touching the alarm).",
        "data": {
          "host": "IP address",
          "port": "Port",
          "password": "Password (4 to 6 digits)",
          "amt8000_mode": "AMT 8000 (experimental protocol)",
          "code_required_arm": "Ask for the password to ARM via Home Assistant",
          "code_required_disarm": "Ask for the password to DISARM via Home Assistant",
          "enabled_zones": "Zones enabled by default",
          "receptor_ip_enabled": "Enable event receiving (Receptor IP)",
          "receptor_ip_port": "Receptor IP listening port",
          "legacy_eeprom_password": "AMT Remoto app access password (6 digits, optional)",
          "anm24_g2_mode": "ANM 24 Net G2 (local protocol)"
        },
        "data_description": {
          "amt8000_mode": "Only check this if your panel is an AMT 8000 — it uses a separate protocol still under development and testing (see README_DETALHADO.md, \"AMT 8000 (experimental)\" section). Leave unchecked for the other models (2018/1016/4010), which keep automatic detection.",
          "code_required_arm": "Unchecked (default): arms with a single tap, no typing. Checked: asks for the password before arming.",
          "code_required_disarm": "Unchecked (default): disarms with a single tap, no typing. Checked: asks for the password before disarming — recommended if you want an extra lock on this action, the more sensitive of the two.",
          "enabled_zones": "Format: ranges and/or numbers separated by ; (e.g. 1-5;8;10-15). Zones outside this list still exist, just disabled until you enable them manually under Settings → Entities.",
          "receptor_ip_enabled": "Unchecked (default): off, no port is opened. Checked: the panel can connect here and push events in real time — you'll need to configure the panel separately (outside this screen) to point at Home Assistant's IP and the port below. See the README for the full walkthrough.",
          "receptor_ip_port": "Port where Home Assistant will wait for the panel to connect (not to be confused with the panel's own port, above) — must match what's configured on the panel. Only matters if the option above is checked.",
          "legacy_eeprom_password": "Leave blank (default) to not use. Only needed if your model/firmware does NOT appear in the README's \"zone names and event log\" list (e.g. AMT 1016 NET with older firmware), or to read the source/battery voltage. It's the same 6-digit password (\"Senha Acesso Remoto\").",
          "anm24_g2_mode": "Check this if your panel is an ANM 24 Net G2. It does not answer automatic detection: it speaks its own local protocol, different from the 2018/1016/4010 models. It serves one local session at a time — close AMT Remoto before connecting."
        }
      },
      "partition_passwords": {
        "title": "Per-partition passwords (optional)",
        "description": "If your 4010 panel has different passwords registered per partition, enter them here. Leave any blank — that partition falls back to the main password from the previous screen. This doesn't replace the 'require code' options: if checked, the value typed in Home Assistant still takes priority.",
        "data": {
          "password_a": "Partition A password",
          "password_b": "Partition B password",
          "password_c": "Partition C password",
          "password_d": "Partition D password"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to the panel. Check the IP, port and password.",
      "invalid_password": "Password must be 4 to 6 numeric digits.",
      "invalid_zone_spec": "Invalid format. Use ranges and/or numbers separated by ; (e.g. 1-5;8;10-15).",
      "unknown": "Unexpected error.",
      "invalid_legacy_eeprom_password": "Invalid password — leave blank or enter exactly 6 numeric digits"
    },
    "abort": {
      "already_configured": "This panel is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure the Intelbras panel",
        "description": "Adjust the password, the require-code-in-UI options, and the polling interval — without removing and re-adding the integration. IP, port, the detected model and the default enabled zones aren't editable here (that last one only takes effect during initial setup, before zone entities exist — remove and re-add the integration if you need to change it).",
        "data": {
          "password": "Password (4 to 6 digits)",
          "code_required_arm": "Ask for the password to ARM via Home Assistant",
          "code_required_disarm": "Ask for the password to DISARM via Home Assistant",
          "polling_interval": "Status polling interval (seconds)",
          "receptor_ip_enabled": "Enable event receiving (Receptor IP)",
          "receptor_ip_port": "Receptor IP listening port",
          "legacy_eeprom_password": "AMT Remoto app access password (6 digits, optional)"
        },
        "data_description": {
          "password": "The new password is validated against the panel itself before being saved.",
          "code_required_arm": "Unchecked: arms with a single tap, no typing. Checked: asks for the password before arming.",
          "code_required_disarm": "Unchecked: disarms with a single tap, no typing. Checked: asks for the password before disarming.",
          "receptor_ip_enabled": "Unchecked (default): off, no port is opened. Checked: the panel can connect here and push events in real time — you'll need to configure the panel separately (outside this screen) to point at Home Assistant's IP and the port below. See the README for the full walkthrough.",
          "receptor_ip_port": "Port where Home Assistant will wait for the panel to connect — must match what's configured on the panel. Only matters if the option above is checked.",
          "legacy_eeprom_password": "Leave blank (default) to not use. Only needed if your model/firmware does NOT appear in the README's \"zone names and event log\" list (e.g. AMT 1016 NET with older firmware), or to read the source/battery voltage. It's the same 6-digit password (\"Senha Acesso Remoto\")."
        }
      },
      "partition_passwords": {
        "title": "Per-partition passwords (optional)",
        "description": "Leave any blank — that partition falls back to the main password from the previous screen.",
        "data": {
          "password_a": "Partition A password",
          "password_b": "Partition B password",
          "password_c": "Partition C password",
          "password_d": "Partition D password"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not validate the new password against the panel. Check that it's reachable (and that the \"Connection\" switch is on).",
      "invalid_password": "Password must be 4 to 6 numeric digits.",
      "password_rejected": "The panel rejected that password (\"incorrect password\") — double-check it.",
      "unknown": "Unexpected error.",
      "invalid_legacy_eeprom_password": "Invalid password — leave blank or enter exactly 6 numeric digits"
    }
  }
}
